apiVersion: v1
kind: Service
metadata:
  name: {{ include "AEV-PDF.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "AEV-PDF.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "AEV-PDF.selectorLabels" . | nindent 4 }}
