apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AEV-PDF.name" . }}-test-connection"
  labels:
    {{- include "AEV-PDF.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', 'http://{{ include "AEV-PDF.name" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
